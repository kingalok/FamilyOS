# FamilyOS Agent

Python agent scaffold for the shared FamilyOS data model.

Install from the `agent/` directory with:

```bash
uv sync
```

Or:

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -e .
```
